<%= form_for(@daily_exercise) do |f| %>
  <% if @daily_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_exercise.errors.count, "error") %> prohibited this daily_exercise from being saved:</h2>

      <ul>
      <% @daily_exercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise_type_id %><br>
    <%= f.number_field :exercise_type_id %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.number_field :duration %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
