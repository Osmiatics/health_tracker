<%= form_for(@daily_step) do |f| %>
  <% if @daily_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_step.errors.count, "error") %> prohibited this daily_step from being saved:</h2>

      <ul>
      <% @daily_step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
